import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
 
  title = 'coffee-shop-bio';

  // Petit rappel : D'abord service ensuite container(view).ts ensuite app.routing.ts

}
